# Insert token secrets for JWT validation
ACCESS_TOKEN_SECRET=

# Insert Redis DNS service name & port (k8s or docker-compose)
REDIS_SERVICE_NAME=
REDIS_PORT=

# Insert database gRPC DNS service name & port (k8s or docker-compose)
DATABASE_SERVICE_NAME=
DATABASE_PORT=

# Insert Kafka DNS service name & port (k8s or docker-compose)
KAFKA_SERVICE_NAME=
KAFKA_PORT=
